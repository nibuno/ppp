# PDF Presenter Pacemaker 技術仕様

この文書はバージョン0.2.0の実装を対象とします。

## 目的

PDFを使った発表中に、発表者がスライド、残り時間、トークメモ、進捗のずれを一画面で確認できるようにします。観客には別ウインドウでスライドだけを表示します。

## アプリ構成

PPPはTauri 2のデスクトップアプリです。

- React/TypeScript: セットアップ、発表者、投影の各画面とPDF描画
- Rust/Tauri: 発表状態の管理、ウインドウ間同期、投影ウインドウの生成
- PDF.js: PDFの解析とCanvas描画
- TanStack Router: 画面ルーティング
- TanStack Query: Tauri側スナップショットの取得と更新

ブラウザ単体での利用はサポートしません。Viteの開発サーバーはTauri開発モードから利用します。

## 画面

### 発表準備 `/setup`

- PDFファイルの選択またはドロップ
- 発表時間の入力
- ページ別トークメモの入力
- PDF読み込み状態とエラーの表示

### 発表者画面 `/presenter`

- 現在ページのPDF表示
- ページ移動
- 経過時間と残り時間
- タイマーの開始、一時停止、リセット
- 時間進捗とスライド進捗
- 現在ページのトークメモ
- 投影ウインドウの起動
- フルスクリーン表示

### 投影画面 `/audience`

- 現在ページのPDFだけを表示
- ページと表示内容は発表者画面に同期
- 状態を変更する操作は持たない

## 発表状態

共有状態はRust側のメモリに保持します。

- 現在ページと総ページ数
- 発表時間、経過時間、残り時間
- タイマー状態（`idle`、`running`、`paused`）
- PDFバイナリとファイル名
- 更新順を表すリビジョン番号

状態が変わると `presentation-state-changed` イベントを全ウインドウへ送信します。画面側はリビジョン番号を比較し、遅れて届いた古い状態で表示を巻き戻さないようにします。タイマー実行中は250ミリ秒ごとに最新状態を取得します。

## Tauriコマンド

- `get_presentation_snapshot`: 現在の状態を取得
- `configure_presentation`: ページ数、発表時間、ファイル名を設定
- `set_pdf_data`: PDFバイナリを保存
- `get_pdf_data`: 保存済みPDFを取得
- `start_presentation`: タイマーを開始または再開
- `pause_presentation`: タイマーを一時停止
- `reset_presentation`: タイマーを初期状態へ戻す
- `go_to_page`: 現在ページを変更
- `open_audience_window`: 投影ウインドウを開く
- `close_audience_window`: 投影ウインドウを閉じる

状態を変更するコマンドは発表者ウインドウからのみ実行できます。PDFは200MB以下かつ先頭が `%PDF-` のデータを受け付けます。

## トークメモ形式

Markdownのレベル1見出し `# ページ番号` を区切りとして解析します。見出しから次のページ見出しまでが、そのページのメモです。メモは発表者画面にだけ表示します。

## 保存

現バージョンではPDF、トークメモ、発表設定、リハーサル結果を永続化しません。アプリを終了すると状態は失われます。

## 検証

- ESLintによるTypeScript/Reactの静的検査
- Vitestによるスナップショット統合ロジックのテスト
- TypeScriptとViteの本番ビルド
- rustfmt、Clippy、Cargo testによるRust側の検証

実行コマンドは `README.md` を参照してください。
